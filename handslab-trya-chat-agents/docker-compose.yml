version: '3.8'

services:
  valkey:
    image: valkey/valkey:latest
    container_name: triagem-valkey-local
    ports:
      - "6379:6379"
    command: valkey-server --save 60 1 --loglevel warning
    volumes:
      - valkey-data:/data
    restart: unless-stopped

volumes:
  valkey-data:
