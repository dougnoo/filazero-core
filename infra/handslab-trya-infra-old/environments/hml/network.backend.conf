# Backend configuration for network stack - HML
bucket         = "trya-terraform-state"
key            = "hml/network/terraform.tfstate"
region         = "sa-east-1"
encrypt        = true
dynamodb_table = "trya-terraform-locks"
profile        = "skopia"
